#Ask the user for:  Principal (P), Rate (R), Time (T). Convert all to  and compute simple interest: 
#SI =(P ∗R∗T)/100

a=float(input('Enter your principal value :'))
b=float(input('Enter your rate :'))
c=float(input("Enter yourv Time :"))

si=(a*b*c)/100

print("simple interest is :",si)